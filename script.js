const btn = document.getElementById("startBtn");
const response = document.getElementById("response");

btn.addEventListener("click", () => {
  response.textContent = "Interesting choice. The developer is now responsible.";
});
